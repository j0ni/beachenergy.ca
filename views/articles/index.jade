for article in articles
  h2 #{article.title}

  | !{markdown(article.content, 500)}

  .article-meta
    small posted #{article.created_at.toDateString()} -&nbsp;
      a(href='/articles/'+article.slug) link to full article
